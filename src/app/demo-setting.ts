export interface DemoSetting {
    zoomMode: string;
    magnification: number;
    scrollZoom: boolean;
    lensMode: boolean;
    lensWidth: number;
    lensHeight: number;
    circularLens: boolean;
}
